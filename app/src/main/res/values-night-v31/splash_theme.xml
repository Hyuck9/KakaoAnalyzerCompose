<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash Screen Theme. -->
    <style name="SplashScreenTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/colorBackgroundSplash</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_logo</item>
        <item name="windowSplashScreenAnimationDuration">800</item>
        <item name="postSplashScreenTheme">@style/Theme.KakaoAnalyzerCompose</item>

        <item name="android:statusBarColor">@color/colorBackgroundSplash</item>
        <item name="android:navigationBarColor">@color/colorBackgroundSplash</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>